{
  "name": "obsidian-slasher",
  "version": "0.1.1",
  "description": "Create custom slash commands that insert custom text based on templates. Insert dates, shell command output, processed clipboard content, and more.",
  "main": "main.js",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "dev-deploy": "npm run build && node scripts/deploy.mjs dev",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "prod-deploy": "npm run build && node scripts/deploy.mjs prod",
    "test": "node --import tsx --test test/**/*.test.ts"
  },
  "keywords": [
    "obsidian",
    "obsidian-plugin"
  ],
  "author": "binnyva",
  "license": "MIT",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^24.6.0",
    "esbuild": "^0.25.0",
    "eslint": "^10.2.0",
    "globals": "^17.5.0",
    "obsidian": "^1.8.10",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.58.2"
  },
  "dependencies": {
    "date-fns": "^4.1.0"
  }
}
